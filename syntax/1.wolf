function add(x,y) x + y

z = add(1, 2)

function multiply(x, y) {
    x * y
}

a = multiply(2, 2)

a = a + z * 2

class Pair {
    x, y

    function new(x,y) Pair {x = x, y = y}

    function add(self) self.x + self.y
}

something = Pair.new(14, 48)

a = something.add()